
 
for (let mult = 1; mult <= 10; mult++) {
    console.log(`---------Número ${mult}----------`);  

    let por = 1;
    while ( por <= 10) {
        console.log(`${mult} x ${por} = ${mult * por}`);
        por++;
    }
}
